{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://localhost:5432/event_planner_db"
      ],
      "env": {
        "PGUSER": "zeeshan",
        "PGDATABASE": "event_planner_db"
      }
    }
  }
}
